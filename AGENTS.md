install the packages: autoconf automake libtool gettext bison texinfo \
  gcc make libgpg-error-dev libgcrypt20-dev

After installing run:
  ./autogen.sh --force && ./configure --enable-maintainer-mode && make
